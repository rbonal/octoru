{
  "_comment": "Token + run ledger by month. Builder checks the current month against monthly_token_cap.",
  "2026-06": { "tokens": 0, "runs": 3, "pages_built": 0 },
  "last_run": {
    "at": "2026-06-01",
    "status": "blocked",
    "reason": "Cowork swapped .mcp.json to maintained google-maps-mcp-server (tools places_search_text, places_details, Places API New). But this running session still does not see those tools (ToolSearch select: -> no match). .mcp.json changes do not hot-reload; the session must be fully restarted AFTER the swap (and the project server approved). No ratings pulled, no pages built.",
    "pages_built": 0,
    "pages_skipped": 0,
    "_token_note": "Token usage not self-measured by the builder this run; cap enforcement reads the monthly 'tokens' field, which a metered runner should populate."
  }
}
