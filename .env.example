# Copy to .env and fill. .env is gitignored — never commit real values.
# The autonomous builder must never read or print these (hard-gated in CLAUDE.md);
# they are for the evaluator + deploy step only.

# Measure (evaluator, read-only)
GSC_SERVICE_ACCOUNT_JSON=./service-account.json
GA4_PROPERTY_ID=
BONALTA_CRM_API_KEY=
BONALTA_PAYMENTS_API_KEY=

# Research (optional)
EXA_API_KEY=
GOOGLE_PLACES_API_KEY=

# Alerts / routing
BONALTA_CONNECT_API_KEY=

# Deploy (GATED — used only by the human-triggered deploy step, not the build loop)
DEPLOY_TOKEN=
